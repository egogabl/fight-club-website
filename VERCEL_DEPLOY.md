# 🚀 Инструкция по деплою на Vercel

## Почему Vercel?
- ✅ Создан командой Next.js — нативная поддержка
- ✅ Автоматическая оптимизация для Next.js
- ✅ Бесплатный план с хорошими лимитами
- ✅ Простой деплой за 2 клика
- ✅ Автоматический HTTPS и CDN
- ✅ Preview деплои для каждой ветки

## 📋 Быстрый деплой:

### Вариант 1: Через GitHub (Рекомендуется)

1. **Подключите репозиторий:**
   - Зайдите на [vercel.com](https://vercel.com)
   - Нажмите "Sign Up" или "Log In"
   - Выберите "Import Project"
   - Подключите GitHub аккаунт
   - Выберите репозиторий `fight-club-website`

2. **Настройки (Vercel определит автоматически):**
   - Framework Preset: **Next.js** (автоматически)
   - Root Directory: `./` (по умолчанию)
   - Build Command: `npm run build` (автоматически)
   - Output Directory: `.next` (автоматически)
   - Install Command: `npm install` (автоматически)

3. **Деплой:**
   - Нажмите "Deploy"
   - Подождите 2-3 минуты
   - Получите ссылку: `https://your-project.vercel.app`

### Вариант 2: Через Vercel CLI

```bash
# Установите Vercel CLI
npm i -g vercel

# Войдите в аккаунт
vercel login

# Задеплойте проект
vercel

# Для продакшена
vercel --prod
```

## ⚙️ Настройки проекта:

### Environment Variables (если нужны):
- В Dashboard → Settings → Environment Variables
- Добавьте переменные окружения при необходимости

### Custom Domain:
- В Dashboard → Settings → Domains
- Добавьте свой домен
- Настройте DNS записи согласно инструкции Vercel

## 🔄 Автоматический деплой:

После первого деплоя:
- ✅ Каждый `git push` в `main` → автоматический продакшн деплой
- ✅ Каждый Pull Request → автоматический preview деплой
- ✅ Мгновенные обновления через CDN

## 📊 Мониторинг:

- Vercel Analytics (опционально)
- Логи в реальном времени
- Метрики производительности

## ✅ Преимущества над Netlify:

1. **Нативная поддержка Next.js** — нет проблем с плагинами
2. **Zero Configuration** — работает из коробки
3. **Быстрее деплой** — оптимизированная сборка
4. **Лучшая производительность** — Edge Network
5. **Проще настройка** — меньше конфигурационных файлов

## 🆘 Если что-то пошло не так:

1. **Очистите кэш:**
   - Settings → Builds → Clear Build Cache

2. **Проверьте логи:**
   - Deployments → выберите деплой → View Function Logs

3. **Пересоберите:**
   - Deployments → выберите деплой → Redeploy

## 📝 Что нужно удалить:

После перехода на Vercel можно удалить:
- `netlify.toml` (опционально, не мешает)
- `NETLIFY_SETUP.md` (опционально)

**Важно:** `vercel.json` уже создан и настроен!

